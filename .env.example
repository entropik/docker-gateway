# =============================================================================
# DOCKER GATEWAY - SECRETS ET CONFIGURATION (EXEMPLE)
# =============================================================================
# 1. Copier ce fichier vers .env: cp .env.example .env
# 2. Remplacer toutes les valeurs CHANGEME par vos secrets réels
# 3. Générer de nouveaux secrets (voir instructions ci-dessous)
# =============================================================================

# -----------------------------------------------------------------------------
# Configuration Globale
# -----------------------------------------------------------------------------
TZ=Europe/Paris
DOMAIN=example.com
ADMIN_EMAIL=admin@example.com
ACME_EMAIL=${ADMIN_EMAIL}

# -----------------------------------------------------------------------------
# CrowdSec Bouncer
# -----------------------------------------------------------------------------
# Générer avec: docker exec crowdsec cscli bouncers add traefik-bouncer
CROWDSEC_BOUNCER_API_KEY=CHANGEME_crowdsec_bouncer_api_key_here

# -----------------------------------------------------------------------------
# Duplicati
# -----------------------------------------------------------------------------
# Générer avec: openssl rand -base64 32
DUPLICATI_SETTINGS_ENCRYPTION_KEY=CHANGEME_duplicati_encryption_key_here
# Votre mot de passe administrateur Duplicati (choisir un mot de passe fort)
DUPLICATI_WEBSERVICE_PASSWORD=CHANGEME_duplicati_admin_password_here

# -----------------------------------------------------------------------------
# Heimdall
# -----------------------------------------------------------------------------
# Générer avec: openssl rand -base64 32
# IMPORTANT: Garder le préfixe "base64:"
HEIMDALL_APP_KEY=base64:CHANGEME_heimdall_app_key_here

# =============================================================================
# IMPORTANT: Ne JAMAIS committer le fichier .env sur Git!
# Le .gitignore est configuré pour ignorer .env automatiquement
# =============================================================================
